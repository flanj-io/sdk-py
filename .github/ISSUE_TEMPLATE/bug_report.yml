name: Bug report
description: Something is not working as expected
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting a bug. Do not include real card numbers, secrets or personal data
        in this issue; redact any payloads. A suspected redaction bypass is a security issue:
        report it privately (see `SECURITY.md`), not here.
  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: A clear description of the bug and what you expected instead.
    validations:
      required: true
  - type: textarea
    id: repro
    attributes:
      label: Steps to reproduce
      placeholder: |
        1. …
        2. …
        3. …
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: Version / commit
      placeholder: "0.1.0.dev0, or a commit SHA"
    validations:
      required: true
  - type: textarea
    id: env
    attributes:
      label: Environment
      description: Python version, `mcp` package version, OS, transport (streamable HTTP / stdio), relevant config.
  - type: textarea
    id: logs
    attributes:
      label: Logs / output
      description: Relevant logs. Redact anything sensitive.
      render: shell
